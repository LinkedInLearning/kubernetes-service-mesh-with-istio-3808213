apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: auth-deny-policy
  namespace: test
spec:
  action: DENY
  rules:
  - from:
    - source:
        namespaces: ["dev"]
    to:
    - operation:
        methods: ["POST"]
